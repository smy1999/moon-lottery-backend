<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moon.lottery.infrastructure.dao.IStrategyDao">
    <insert id="insertStrategy">
        INSERT INTO strategy (strategy_id, strategy_desc, strategy_mode, grant_type,
                              grant_date, ext_info, create_time, update_time)
        VALUES (#{strategyId}, #{strategyDesc}, #{strategyMode}, #{grantType}, #{grantDate},
                #{extInfo}, #{createTime}, #{updateTime});
    </insert>


    <select id="queryStrategyByStrategyId" resultType="com.moon.lottery.infrastructure.po.Strategy">
        SELECT id, strategy_id, strategy_desc, strategy_mode, grant_type,
               grant_date, ext_info, create_time, update_time
        FROM strategy
        WHERE strategy_id = #{strategyId}
    </select>
</mapper>